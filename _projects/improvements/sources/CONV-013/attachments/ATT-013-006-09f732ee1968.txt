 Nortropic Digital — Master Plan (Universal Web Competency → Digital Department → Operating System)

 Implementation plan grounded in repository inspection (5 exploration agents), design synthesis (5 planning agents), and live literature/industry/legal search (7 research agents), covering all eight owner directives.

AN PRECEDENCE RULE (coherence patch #12, authoritative): PART 12 EXECUTION ORDER + the OWNER-DECIDED AMENDMENTS (Part 7 DECIDED block) + the FINAL COHERENCE PATCH override any stale wording elsewhere in the narrative plan. No implementation agent may infer authority from an older conflicting paragraph. Where narrative sections and Part 12 disagree, Part 12 wins.

 Context

 Nortropic's website factory is structurally specialized around Swedish local trades. The owner wants: any legitimate website customer → diagnose organization/users/outcomes → compose the right research, strategy, capabilities, quality contract, workforce and evals — with local-service preserved as the first capability pack, not the architecture. Second directive: Webbförvaltningen must acquire real professional mastery — five knowledge layers (theory, practice, craft, frontier, own experience) under knowledge governance, compounding with every project, without turning anecdotes or fashion into truth.

 Part 0 — Current state (verified) and corrections to the mission's assumptions

 The estate

 - nortropic-system = TWO nearly disjoint systems: (a) the customer factory — 7 agents (~667 lines), 10 skills, 9 vendored design-canon skills, 6 workflows, 12-node pipeline, 3 hard human stops (brief/legal/proposals), constitution §A/§B, 22 rules, eval-rubric v3.0.0 (semver, §A2-protected), AUTOPILOT=off; (b) the control-plane loop — controller/verify/specs/config (~18k lines, 100% domain-free by grep), h-tasks with frozen exit tests, allowed_write, attestation, currently mid-bootstrap at h031+.
 - verkstadsgolvet = operator control room (read-only) + IMPLEMENTED onboarding (single page, 7 free-text fields, copy-prompt variant A — owner runs research manually in browser-Claude, pastes research.md back, app creates private kund-<slug> repo; the repo IS the customer record; app NEVER starts builds). Research prompt inlined in lib/prompt-research.ts ("v2"). Leads-prospecting module (38 Swedish trades, 290 kommuner) is separate.
 - nortropic-knowledge = institutional knowledge, non-authoritative BY CONSTRUCTION (NORMATIVE_SECOND_SOURCE_OF_TRUTH=FORBIDDEN, RUNTIME/VERIFICATION_DEPENDENCY_ON_KNOWLEDGE_REPO=FORBIDDEN). Mostly empty scaffolding + ONE 1218-line RFC (improvements corpus) containing designed-but-unbuilt analogs of directive II (see Part 2).
 - innovation-intake = harvested brainstorm corpus incl. three frontier-watch briefs; three live watch feeds run (daily Frontier Delta, daily Bevaka, weekly /design watch).

 Corrections to the mission prompt (repo contradicts assumptions)

 1. Research is human-executed today (node 1: "operatören skriver filen" via browser-Claude with the composed prompt). The domain intelligence lives in the planner (§7 Kalibreringsprofil), not a research agent. Server-side research (variant B) was deliberately not built (commit b3bcc7f).
 2. No customer has ever run through the pipeline on this machine. Zero research.md, zero kund-* repos locally; the one production-ish site (rorjour-stockholm, the frozen 95/100 eval baseline) was built on another machine; ~/Workflow (profiles, fixtures) is absent here; verify-suite probes are currently UNDECIDABLE on this machine. FIRST-REAL-LAUNCH ceremony never performed. The "customer factory lane" in verkstadsgolvet is showroom-only (LANE-02 in a worktree, not at HEAD).
 3. The rubric already contains the generalization pattern: v1.2.0 neutralized criteria 1/3/5/6/9 from hantverkar-assumptions into profile.ts-derived sub-criteria at identical requirement level ("andra arketyper får härledda delkrav på samma nivå"); criterion 5 has a varumarke mode; criterion 6 supports non-LocalBusiness types. The plan EXTENDS this pattern.
 4. Ring 3 refusal (ecommerce/stateful/membership) is a deliberate, §A5-adjacent business decision — statelessness is the declared moat ("stateless sajter som aldrig kräver jour — det är vallgraven") and the single-operator survival condition. The universal architecture models these as ROUTE-OUT capabilities with named referrals; it does not silently override.
 5. The loop cannot implement this work: "Kontrollplanet rör aldrig kundflödet" (docs/loop/regler.md:21) + workflows/skills in denied_write ⇒ ALL slices ship via the OWNER-AUTHORED channel (batches, human approval, HÖGRISK where §A surfaces change).

 Assumption debt (where local-service is baked into "generic" machinery)

 Concentrated in exactly five places: (1) docs/03-regelverk.md:37 invariant list (ONE primärhandling, max 5 fields, NAP, static-first); (2) eval-rubric.md — 24p effectively pack-bound (NAP 8, Lokal SEO 8, svensk schema-detaljer 8) after v1.2.0's parameterization of the rest; (3) nortropic-prelaunch gates 0–7 (Gate 5 local content); (4) nortropic-stack + file-structure (fixed Swedish routes, NAP-sacred business.ts); (5) project-planner.md §7 contract (hantverkar-defaults, closed primärhandling-enum, "[tjänst] i [stad]", Swedish-market-only). Plus: launch.js gate prompts + leadgen category enum; review.js second skeptic's lens ("Swedish local-service lead-gen site"); research prompt v2 (F-skatt/ROT/Reco/IG steps, 5-field kontrollrad demanding telefon+ort); onboarding "bransch + huvudort" field; verify-suite frozen preview = a customer site; totally-local files (nortropic-seo-lokal, GBP/GSC checklists, juridikflaggor, legal-requirements-se) — these are correct as PACK CONTENT, wrong only as universals.

 Universal assets to preserve unchanged

 Fact-discipline (belägg/[OSÄKER]/konflikt-notering/read-only/fakta-inte-slutsatser) · faktatrohet hard gate · adversarial 2-skeptic verify · tri-state verdicts at 5 layers (ODÖMBART never green) · anchor requirement (V4-läxan) · BATCH-005 fix contract · antislop 3-layer stack (blocklist→PK→bevis) · premium-bevis curation law (one-trait-per-exemplar, diversity guard, on-demand expansion) · kluster-differentiation lens 9 (only anti-homogenization mechanism) · bild claim rules R1–R4 · 5f self-test · one-home principle · vendored-deps-with-hashes · semver'd contracts with doctor drift guards · steward retro pattern gate ("twice across projects") · §B8 Goodhart meta-review.

 Part 1 — Target architecture (universal competency)

 Governing decisions

 - D1 No new pipeline nodes. Universal intake→node 1; capability discovery+strategy+quality contract→node 2 (inside PROJECT-BRIEF); "we understand your project like this"→node 3 (existing HÅRT STOPP); eval composition→node 7. Every existing STOP keeps its meaning; deploy stays human; legal stays human-only.
 - D2 Capabilities before archetypes; catalog before graph. v1 ships a capability CATALOG (markdown table with stable IDs, statuses, activation signals, krav-pointers) — a graph file format waits for a mechanical consumer and ≥3 interdependent packs (red-team verdict: no consumer exists at n=1). Archetypes = named capability compositions in pack strategy modules, never hard boxes.
 - D3 Extend the existing seams, never sibling artifacts. research.md contract (versioned v3) · brief §7 (grows §7.10–7.14) · content/profile.ts (v2.0.0 = THE Site Quality Contract carrier; no parallel JSON — profile.ts is already the declared transport, semver'd, doctor-guarded, gate-read) · the GATES array in launch.js (parameterized, not replaced) · the rubric's own v1.2.0 neutralization pattern (extended to core+modules).
 - D4 Fail-closed capability honesty. Statuses: BUILT/VALIDATING/PROVEN/DECLARED/ROUTE-OUT (no "CERTIFIED" — implies external authority; promotion threshold deliberately identical to the existing Sonnet-trappan: ≥2 consecutive real customers, eval ≥90 same rubric MAJOR, zero post-launch gate misses, verify-suite green; demotion mirror on first miss). A KRÄVS signal on ROUTE-OUT ⇒ existing scope-nej flow (STRATEGISK question, obemannat stops, referral). On DECLARED/VALIDATING ⇒ honest brief wording: "kapacitet X ej tillräckligt validerad — offereras separat eller valideras i detta projekt med ägarbeslut."
 - D5 Version detection, never silent reinterpretation. research.md without Kontrakt:-row = v2 = LOCAL implied, legacy INPUT GATE. profile.ts <v2.0.0 = paket:'local' (BC-1, logged). Old customer repos stay readable forever; backfill only at rebuild.
 - D6 Owner-authored channel for everything (Part 0 correction 5). §A-touching changes batched into few HÖGRISK sittings.
 - D7 One primary action = the v1 operating default/invariant, explicitly recorded as CHALLENGEABLE by future real evidence (amendment #5). The active fixtures satisfy it (Case B SaaS: boka demo; the DEFERRED museum fixture would too: planera besöket). The deeper universal contract is PRIORITIZED OUTCOMES + TOP TASKS + MANDATORY JOURNEYS — already carried by §7.10–7.14/profile.ts v2 — and the plan does NOT silently assume every future legitimate website can forever be represented by exactly one equally dominant action. Weakening the invariant remains a HÖGRISK invariant-list change, triggered by a real signed customer whose brief genuinely cannot express one primary action; the challenge path is pre-recorded, not improvised.
 - D8 Statelessness moat stays. Mechanical test unchanged (Del-C static-guard). Sharp boundary formulation: "does the site repo hold state the operator must steward? yes = software product (route out / separate quote), no = website." External booking/ticketing/checkout embeds and links = inside; one clarifying sentence in 06-scope re localStorage/external-SaaS state (OWNER-DECISION).

 1. Universal Research Core (research contract v3)

 - One-home fix (ownership RESOLVED by pre-execution amendment #2): the contract moves to nortropic-system as skills/nortropic-research/references/research-contract.md (semver v3.0.0, changelog — the eval-rubric pattern); verkstadsgolvet/lib/prompt-research.ts becomes a thin composer (prompt-core.ts + prompt-packs/local.ts) carrying vendored pinned bytes with exact commit + hash (KONTRAKT-SHA) — NO mutable runtime GitHub branch reads may ever become professional authority; freshness enters exclusively through Radar → candidate → verification → promotion.
 - v3 sections (universal spine; discipline verbatim from v2: belägg-per-påstående, [OSÄKER], konflikt noteras aldrig väljs, read-only, fakta-inte-slutsatser): 1 Organisation & kontakt (kontaktvägar TYPED: telefon/e-post/plats/bokningslänk) · 2 Erbjudande · 3 Användare & målgrupper (NEW) · 4 Toppuppgifter & primärt utfall (candidate mapping FREE-FORM in core; the closed enum ring/boka/platsförfrågan/offert/besök becomes LOCAL-pack vocabulary) · 5 Geografi & språk · 6 Förtroende & bevis (kvitton generalized; NYSTARTAD mode verbatim) · 7 Innehåll & bildmaterial (+Bild-URL:er) · 8 Röst & varumärke · 9 Transaktion & data (RAW observation, quotes, no judgments) (NEW) · 10 Integrationer (NEW) · 11 Juridik-/risk-observationer (unchanged) · 12 Konkurrenter/alternativ ("vad jämför användaren med") · 13 Designreferenser (universal recipe; Reco≥4.7-hunt → LOCAL module) · 14 Framgångsmått (NEW) · 15 Kapacitetssignaler (mechanical table: kapacitet-id · signal/citat · KRÄVS/OBSERVERAD/MÖJLIG) (NEW) · 16 Öppna frågor · 17 Kontrollrad. Pack modules append under ## PAKETMODULER (<id>@version) with prefixed section IDs — universal numbering never shifts. The composed LOCAL prompt ≈ today's length (new universal sections are short; local bulk moves out); universal-only prompt is SHORTER than today's (answers the slow-onboarding red-team attack).
 - Kontrollrad generalization (regel 5 re-wording — HÖGRISK batch): universal mandatory 6 = namn · ≥1 verifierad kontaktväg (typed) · ≥1 erbjudande · ≥1 användargrupp · ≥1 primärt-utfall-kandidat (may be [OSÄKER], may not be absent) · ≥1 förtroendeunderlag ELLER explicit NYSTARTAD. Pack rows are additive-only/monotonic (LOCAL adds telefon + ≥1 ort) — packs can only TIGHTEN the gate (gaming guard; preserves Ring-1 depth mechanically). Machine-parseable row format; planner INPUT GATE stops on any SAKNAS exactly as today.
 - Discovery chicken-and-egg: packs pre-activated at onboarding from cheap heuristics + operator hypothesis (tri-state Lokal/Annat/Osäker; Osäker ⇒ core-only prompt); §15 then does evidence-derived discovery against the full catalog; a signal matching a non-activated pack ⇒ automatic SAKNAS-row ("komplettera researchen") — fail-closed, never silently thin.

 2. Capability catalog (v1) — packs/kärna/kapacitetskatalog.md

 - Definition: a capability = a unit of site competency the factory can deliver AND independently verify — above calibration values, below industries. Anti-tag guard (mechanical): an entry is invalid without ≥1 activation signal, ≥1 verifiable krav with a hem (existing gate/eval anchor), and a status. Names functional, never industrial (litmus: "skulle två företag i olika branscher behöva samma kapacitet?"). Universal always-on qualities (faktatrohet, a11y, security) are NOT entries — they live in the rubric core.
 - Initial population from repo evidence — VALIDATING* (rorjour-proven but see status honesty): ring-konvertering, lead-formular, lokal-sokbarhet, gbp-narvaro, citations-katalog, omdomen-visning, karta-embed, rot-prisvisning · BUILT (machinery exists, never shipped): extern-bokning, hitta-hit-besok, portfolio-galleri · DECLARED: flersprakighet, nyhetssektion-statisk, statisk-priskalkylator · ROUTE-OUT (with named referrals per 06-scope): kundvagn, kassa-betalning, kundinloggning, medlemskap, bokningsmotor-egen, anvandargenererat-innehall, foreningsplattform.
 - Honest status of the local-service pack itself: VALIDATING with an asterisk (one production-ish site, built on another machine, FIRST-REAL-LAUNCH never performed, verify-suite undecidable here). Not "certified".
 - Statefulness invariant: stateful ⇒ ROUTE-OUT, enforced structurally (mirror of Del-C guard). JSON mirror + doctor checks: DEFERRED until a mechanical consumer exists.

 3. Capability packs + LOCAL extraction — packs/lokal-se/

 - Layout: PAKET.md (manifest: id, version, status, activation heuristics+signals, capability list, kontrollrad additions, deliverables, agent fragments, profilDefaults) · research-modul.md (F-skatt/ROT/Reco/NAP/lokala konkurrenter/primärhandlings-vokabulär) · strategi-modul.md (hantverkar-defaults, "[tjänst] i [stad]", svensk sajtarkitektur, per-bransch profildefaults — becomes the new home of the ~/Workflow/profiler/ library, OWNER-DECISION D) · eval-modul.md (§A2-classed) · grind-linser.md (§A3-classed) · fragments/ per agent.
 - One-home rulings: nortropic-seo-lokal STAYS at skills/ (harness-loadable), stamped Pack: lokal-se, manifest points. juridikflaggor rows DO NOT move (§A4 single home; packs point). GBP/GSC checklists stay in seo-lokal refs; manifest lists them as pack deliverables (emitted only when paket=lokal-se).
 - §A split inside packs: only eval-modul.md + grind-linser.md are measuring-stick files (HÖGRISK, doctor #5 grep extended); everything else = normal owner commits, steward may propose. This is what makes routine variation never touch §A.
 - .gitignore whitelist additions required: !/packs/ (nothing commits otherwise).

 4. Nortropic Website Standard (eval-rubric v4.0.0 — designed now, SHIPPED at trigger)

 - Shape: UNIVERSAL CORE 76p, 8 criteria (Utfallsarkitektur 15 — renamed Konverteringsarkitektur, derives from primarUtfall/gate1Test, 5-second-test as scoring guidance · Faktatrohet 15 HÅRD GRIND · Copykvalitet 10 (language per contract, svenska default) · Prestanda 8 · Juridik 8 (SE/EU baseline universal for this operator) · Visuell distinktion 10 · Förtroendesignaler 5 · Teknisk hygien 5) + MODULE SLOT exactly 24p filled by contract-required pack modules (natural weights, proportional scaling stamped in EVAL-RESULT header); zero-module projects score defaults schema-bas 12 + sokbarhet-bas 12. LOCAL modules re-house v3 criteria 4/5/6 at equivalent requirement level and weight — v4 must preserve the important local-service SEMANTICS and DEFECT SENSITIVITY; exact reproduction of the historical 95/100 total is a DIAGNOSTIC, never a promotion requirement (amendment #4: never optimize v4 to numerically imitate v3 — v3 and v4 remain separate measurement regimes; a diagnostic divergence is investigated, not tuned away).
 - Evidence classes mapped to EXISTING machinery, no new engine: HARD_GATE=launch gates+faktatrohet · QUALITY_EVAL=rubric core+modules (informative, retro-compared) · HUMAN_JUDGMENT=Gate 6/nod 3/nod 8/status promotions/rubric changes · POST_LAUNCH_OUTCOME=framgangsmatt read at retro.
 - Timing (OWNER-DECIDED 2026-08-24 — draft-now, activate-at-real-consumer, with measurement-regime discipline): v4 is drafted now marked EXPERIMENTAL/NOT_PRODUCTION; v3 stays the authoritative production rubric and steward baseline. The activation trigger is NOT mechanically "first non-local customer signs" — it is: THE FIRST PRODUCTION PROJECT WHOSE QUALITY CONTRACT CANNOT BE CORRECTLY EVALUATED BY v3 WITHOUT DISTORTION, or an explicit owner go-live order. Transition discipline: (1) backtest v4 against the historical fixture corpus — the purpose is NOT numerical imitation of v3 but: same obvious winners/losers? important defects still detected? any dimension diluted? local pack faithfully preserves the mature expertise? does the universal core reward irrelevant qualities? can a strong universal score hide a catastrophic pack failure? (2) paired bridge dataset: v3+v4 results retained on a fixed corpus to OBSERVE the discontinuity — no conversion formula ever derived (MAJOR versions are different measurement systems); (3) v3 series is immutable history — never rewritten into v4; longitudinal analysis crosses the boundary only on stable underlying dimensions/outcomes, never raw totals; (4) at activation: HÖGRISK authorization → MAJOR commit → freeze rubric+pack identities → recut authoritative baselines → record an explicit MEASUREMENT_REGIME_CHANGE; (5) short bounded v3 SHADOW dual-run on the first local-service sample after cutover (regression detection on the historic signal — never an indefinite second authority); (6) steward/gym/experience are rubric-version-aware: never pool raw totals across v3.x/v4.x; every quality observation carries rubric major/minor + pack id/version + gate version + fixture regime; (7) the 24p pack slot must not become a loophole: a missing required pack, wrong pack, or catastrophic pack-specific failure is NOT compensable by the 76p core — applicability + hard-failure semantics defined BEFORE v4 promotion; (8) v4 = challenger, v3 = incumbent — the same promotion discipline as models/skills/tools.

 5. Site Quality Contract = content/profile.ts v2.0.0 (extend, NO sibling artifact)

 - New/changed fields: profilKontraktVersion v2.0.0 · katalogVersion stamp · paket[] · primarUtfall{typ: kapacitet-id (open, validated against catalog — replaces closed enum), etikett, gate1Test, harledning} · anvandargrupper[] · toppuppgifter[] · kapaciteter[{id, status-SNAPSHOT, beslut: KRÄVS/AVBÖJD/ROUTE-OUT, harledning}] · fortroendekrav{kvitton (absorbs v1), attribution, harledning} · obligatoriskaResor[{id, beskrivning, testbar}] · forbjudnaPastaenden[] · kravNivaer{a11y: WCAG22-AA, perfRef, seoLage} · integrationer[] · framgangsmatt[{beskrivning, matkalla: analytics-event/gsc/gbp/kundrapport, klass: POST_LAUNCH_OUTCOME}] · olostaOkanda[{text, klass}] · kontraktGodkand{datum, briefCommit}|null. Kept: schemaTyp, juridikflaggor, rostregister, branschAntislop, motionNiva, noindexCutover.
 - Provenance: Harledning = {typ:'FAKTA', ref:'research.md §id'} | {typ:'STRATEGI', ref:'PROJECT-BRIEF.md §n'} — pointers only, on claim-bearing fields only.
 - Immutability: nod-3 approval sets kontraktGodkand; launch Freshness phase gains a mechanical check — profile.ts changed after briefCommit without renewed approval ⇒ BLOCKED-STALE (same shape as the review-freshness gate; part of the batched §A3 edit).
 - Migration hazard flagged: doctor #5 semver formula makes v1-stamped frozen fixtures incompatible with a v2 contract — needs a pinned-MAJOR exemption or fixture re-cut (OWNER-DECISION G).

 6. Strategy compilation = planner evolution (NO new agent, NO new stage)

 Research/strategy separation preserved by CONTRACT, not by stage: research.md carries only facts+signals; the brief carries all decisions. Planner changes: INPUT GATE reads the versioned contract (universal 6 + activated-pack rows) · new step 2b Kapacitetskompilering (research §15 × catalog → per-signal beslut in §7; ROUTE-OUT-KRÄVS ⇒ scope-nej flow; BUILT/DECLARED-KRÄVS ⇒ STRATEGISK fråga, obemannat stops — reuses regel 21 verbatim) · §7 grows 7.10 Kapaciteter, 7.11 Toppuppgifter & obligatoriska resor, 7.12 Framgångsmått, 7.13 Förbjudna påståenden, 7.14 Olösta okända ("exactly 7 sections" invariant intact) · local prose replaced by Read-pointers to packs/<id>/strategi-modul.md (exactly how inspirationsprotokollet is bound today).

 7. Onboarding integration (verkstadsgolvet — app channel, not normative)

 Single page stays; variant A (copy-prompt) stays — NOT reopened. Field evolution into 4 groups: Om verksamheten (kundnamn* + fri beskrivning — absorbs "bransch+huvudort"; ort becomes evidence, not form token) · Vad sajten ska åstadkomma (free text; enums live downstream) · Vem använder den · Befintligt material, kanaler & system (keeps paste-everything formulärsvar* + FB/IG/site + NEW system-field for bokning/biljetter/kassa/CRM). Operator-only Pakethypotes control (Lokal/Annat/Osäker; heuristic default; always overridable). Review surface: after research.md is pasted back, the form renders read-only: Projektförståelse-section (5 lines: organisation · vad sajten ska driva · vem som använder · befintliga system · pakethypotes med belägg — new mandatory closing section of contract v3) + kontrollrad + [OSÄKER]-count + öppna-frågor-count; parse failure renders — and a warning, never blocks, never invents. App still NEVER starts builds; success screen semantics unchanged. Customer-facing "we understand your project like this" stays a manual email in v1. Future autonomous continuation (HIGH CONFIDENCE ⇒ obemannat) already exists as Läge: obemannat + STRATEGISK-question stops — no new trust machinery; the new capability stops plug into it.

 8. Eval composition (gates) — parameterize the EXISTING engine (one batched §A3 edit)

 Universal unchanged: Gate 0 build, 2 perf, 3 responsive, 4 a11y, 7 security. Universal-parameterized: Gate 1 reads primarUtfall (+catalog krav where hem=gate:utfall); Gate 6 legal flag-driven (unchanged). Split: Gate 5 SEO = universal technical core (sitemap/robots/canonical/unique meta) + pack lens appended from grind-linser.md (NAP/GBP/LocalBusiness/IndexNow move there). NEW 8th lens in the SAME GATES array: journeys — composed from obligatoriskaResor + toppuppgifter (secondary journeys: hitta öppettider/pris/hit); reuses parallel structure, fix loop ≤3, BATCH-005 contract, final sweep. Category enum: leadgen→utfall (+journeys), alias kept one release. Review.js second skeptic re-lensed: "does this matter for THIS site's contracted primary outcome, capabilities and top tasks (read content/profile.ts) — or is it pedantry?" Execution classes per krav: MECHANICAL / BROWSER-SYNTHETIC / INDEPENDENT-AI / HUMAN / PRODUCTION-ONLY. Trust review = existing trust gate fed by fortroendekrav; adversarial ship-blocker = existing 2-skeptic verify + gates; 5-second test = criterion-1 scoring guidance. Residual unmitigated risk acknowledged: both skeptics share the builder's model family ⇒ the human eval touchpoint per launch stays (cheap at n≤5); future mitigation = cross-provider challenger (Part 2 M).
 Handover becomes pack-conditional (GBP/GSC deliverables only for lokal-se; pack-appropriate handover otherwise). Cutover pack-aware (GBP/citations phases only lokal-se). Final-touches unchanged. Verify-suite fixture paths repaired in Slice 0.

 9. Workforce composition (ROLE vs COMPETENCY)

 Roster stays exactly 7 agents. ROLE = agent file (tools, model/effort per MODELLKONTRAKT, injection-hardening, memory). COMPETENCY = pack module loaded into a role (planner←strategi-modul; seo-optimizer←nortropic-seo-lokal; qa-launcher←prelaunch pack sections; eval←eval-modul). The planner IS the selector: §7 names per-role modules; downstream agents load conditionally (existing precedents: gsap-build only when chosen, juridikmodul only when flagged, Gate-1 profile-conditional). Constraints honored from corpus DO-NOT-BUILD: #17 no dynamic workflows in the production pipeline (same-shape pipeline, pack-conditional content) · #18 no parallelizing one site build (gestalt needs one context owner). A genuinely NEW agent is warranted only for: different security posture, different model-contract row, or a competency too large to be a module — none in v1.

 10. Competency status model

 Five statuses in ONE home — a status table added to docs/06-scope.md (capability status IS scope status; rings become status-driven, not industry-listed): DECLARED (paper) · BUILT (machinery, zero real customers) · VALIDATING (≥1 real customer launched; EVAL-RESULT + gate history named in the row) · PROVEN (Sonnet-trappan threshold: ≥2 consecutive real customers, eval ≥90 same rubric MAJOR, zero post-launch gate misses, suite green; demotion mirror) · ROUTE-OUT (named referral). Promotion evidence = pointers to existing artifacts ONLY (EVAL-RESULT, launch verdicts, STEWARD-REPORT rows, suite results). LEARNING-RECORD data is explicitly NOT promotion evidence (anti-Goodhart placement law). New-model handling in Part 2 M.

 11. Post-launch outcome loop (minimal honest version)

 Reality: dozens of visits/month ⇒ rates are fiction; counts are not. Meaningful signals at this volume: lead existence (every quote_submit = a Resend email, exact count; "zero leads in 60 days" is a real finding) · GSC queries/impressions · GBP insights · customer's own report (strongest at n=1, currently thrown away). Design: LEARNING-RECORD.md per customer repo, human-written, 4 sections: Hypotes (1–3 lines at handover, derived from §7/framgangsmatt — the ONE "Utfallshypotes" line added to HANDOVER) · Observation (date-stamped RAW COUNTS + period + denominator; rule: counts, never percentages, below 500 sessions/period; filled during the EXISTING monthly GSC retainer routine — ten minutes, no dashboard) · Fynd (tagged ANEKDOT by default) · Generaliserbarhet ("sannolikt bara denna kund" | "kandidat-mönster — jämför nästa kund"). Feeds the existing loop: one added retro input line (steward reads every LEARNING-RECORD in scope); an ANEKDOT lifts only when a second customer shows the same direction, and the proposal must still name its rubric criterion. Placement law: retro input ONLY — never machine-parsed, never a gate input, never promotion evidence. Explicit non-builds: no ingestion pipeline, no metrics store, no dashboards, no weekly cadence.

 12. Website vs software boundary

 Ring model kept, upgraded semantically: Ring 1 = packs at BUILT/VALIDATING/PROVEN · Ring 2 = DECLARED (built on first yes, quoted separately — the existing juridikflagg-ohanterad mechanic, generalized STOP not list) · Ring 3 = ROUTE-OUT rows with named referrals (ecommerce→Shopify; own state→separate systems-development "Railway-klass" quote; föreningar→laget.se). Boundary test (mechanical, exists): Del-C static-guard — only server code is lead.ts, no DB client, no auth. Sharp rule: "does the site repo hold state the operator must steward?" External booking/ticketing/checkout embeds = website; in-repo state = software product. Customer never needs to understand the routing — the planner's ROUTE-OUT flow produces the referral. OWNER-DECISION: one clarifying sentence in 06-scope (client-side-only + external-SaaS-held state inside the moat; server-held state out).

 Part 2 — Professional knowledge & mastery layer

 Grounding: harmonize with the corpus RFC, don't invent parallel vocabulary

 The knowledge repo RFC already designed most of directive II, unbuilt: Knowledge Radar ≙ CF-4 Observatory (sensor mesh, Thesis Ledger, Contradiction Engine, Assumption Registry, Source Alpha) merged with the Evolution Loop's spine — evidence ladder OBSERVED→CORROBORATED→LOCALLY_REPLICATED→LOCALLY_PROVEN→ADOPTED, optimizer-invisible held-out evals, multi-run/shuffled protocol, harness ablation with REMOVED ledger, Strategy Reconsideration Gate, no shadow backlog. Owner ruling on file: Observatory frame supersedes Evolution Loop brief (frame, not rigor). Curriculum Compiler ≙ CF-2 context compilation (corpus's "largest identified multiplier"; naming collision Manifest/Capsule/Pack unresolved §10.1). BoK homes ≙ CF-5 knowledge lanes ("normative state is never memory"). Three live watch feeds already run (daily Frontier Delta, daily Bevaka, weekly /design watch); consolidation = unresolved contradiction #7, owner decision. Binding DO-NOT-BUILD rows: #1 no self-authorizing research agent (radar is propose-only) · #3 no persona swarms (one frontier model + context projection) · #4 watch = notification surface, not product · #10 no giant master context doc · #20 evals code-native/locally reproducible · #23 "best practices" never terminal (pair with first principles) · #27 budget exhaustion ≠ PASS · #28 static source tiers = cold-start prior only; measured Source Alpha = end state.

 Existing embryos (build on, never beside)

 Source registry: VENDORED.md (source/date/hash/load-bearing role, doctor #9 diff) + models.json (doctor #13: <90-day revision, deprecated-model FAIL) + inspirationskallor.md (authority weighting; "Awwwards = antislop list with a prize rosette"). Golden corpus: premium-bevis.md (one-trait-per-exemplar, F1–F6, diversity guard, verification dates, on-demand expansion law). Freshness: "Senast verifierad" stamps + doctor #12/#13. Provenance: knowledge-doc frontmatter (status/authority/provenance/canonical_sources@SHA/supersedes/superseded_by/last_reviewed), append-only beslutslogg, blob-SHA-pinned plan authority. Experience: AGENT-LOG.md (anti-noise filter), EVAL-RESULT per-criterion cross-client comparison, retro pattern gate, verify-kalibrering with mechanical decision rules.

 A–B. Web BoK architecture + taxonomy

 The runtime BoK already exists — it is skills/*/references/ + vendored-skills/, doctor-guarded. The BoK is an ACCOUNTING of that operative layer plus an advisory hinterland, not a new document tree. Three layers: operative core (nortropic-system: skills/references, vendored-skills, packs/, regelverk, rubric — normative, owner-gated, §A HÖGRISK) · advisory hinterland (nortropic-knowledge/bok/ claims-in-flight, curriculum sources, radar archive, experience narratives, KONFLIKT records — authority: none always, NEVER read by any build/verification process) · index (bok/INDEX.md, one page, pointers per GOVERNANCE repo@SHA convention + honest gap notes). Promotion path advisory→operative = the EXISTING steward-proposal channel only. Taxonomy: 8 domains, not the directive's 13 (merged where no distinct consumer exists): plattform (platform+software-eng) · prestanda · tillganglighet · sakerhet (security+privacy) · design (UX/HCI+IA+visual) · innehall · sok · drift-affar (operations+product/business); the 13 labels survive as optional tags. v1 economy: almost no BoK writing — claims created ONLY by radar deltas, experience lessons, and experiments; never by encyclopedia sessions.

 C. Knowledge claim schema + lifecycle

 ONE ladder, the RFC's five states verbatim + three post-adoption states: OBSERVED→CORROBORATED→LOCALLY_REPLICATED→LOCALLY_PROVEN→ADOPTED, then ADOPTED⇄CHALLENGED→DEPRECATED|SUPERSEDED. (Directive-II term mapping recorded in the template: PROFESSIONAL_PRACTICE_CANDIDATE≙CORROBORATED, EXPERIMENTAL≙LOCALLY_REPLICATED, CURRENT≙ADOPTED.) Claim = one file bok/claims/W-<id>-<slug>.md on the existing 9-key frontmatter + exactly 5 new fields, each with a named consumer: claim_id (conflict/supersede refs) · domain (radar routing) · ladder (state machine) · scope MANDATORY (the anti-universalization guard) · operative_home (radar impact analysis). Cut from the directive's ~17: confidence scores, priority, embeddings, per-claim cadence (derived from source volatility), author fields — no consumer. Each claim additionally carries the shelf-life class tag from T-2 (classes 1–7) — this operationalizes timeless-principle vs current-implementation-fact; class-6 platform facts are stored as "fact as of DATE + source URL + re-verify at use", never as static truth. Demotion: radar trigger (source delta hits cited source), experience trigger (production contradicts ADOPTED claim), staleness (health line only, never auto-demotion). All demotion of operative text goes through proposals; §A ⇒ HÖGRISK.

 D. Source registry (nortropic-system/config/kallregister.json)

 Schema per row: id, namn, url, klass (NORMATIVE|PLATFORM|PRACTICE|CRAFT|FRONTIER|INTERNAL), styr (domains/operative paths), volatilitet_dagar, andringsdetektion {typ: npm-version|changelog-url|rss|errata-page|doctor-check|manual, ref}, konsument, tier (declared COLD-START PRIOR), alpha (reserved null — the Source Alpha seam: per-cycle consultation/outcome table in radar reports makes alpha computable at ≥6 cycles; no code in v1). Embryos folded by POINTER (one-home): VENDORED.md rows delegate to doctor #9; models.json to doctor #13; inspirationskallor sources get class/cadence rows while the prose file stays the planner's authority. v1 = ~13 rows total (3 embryo pointers + WCAG/errata, WHATWG review drafts, web-features npm, browser-compat-data npm, Chrome release notes, Search Central RSS, Next.js releases, Tailwind, Baymard, svensk juridik manual). New doctor #14 (tri-state, fail-soft): registry parses; NORMATIVE/PLATFORM rows overdue vs volatilitet ⇒ WARN.

 E–F. Knowledge Radar (Observatory web-instantiation) + gated promotion

 Not a fourth feed — the three live feeds stay as notification surfaces; the radar is the MONTHLY DIGESTION ORGAN: new skill skills/nortropic-radar/ (a skill, NOT an agent), owner-triggered as BOOTSTRAP ONLY (amendment #6): the target state is autonomous read-only observation/change-detection/materiality-classification/proposal-generation on the progressive-autonomy ladder, with the owner seeing only material exceptions and promotions — the propose-only boundary (DO-NOT-BUILD #1) holds at every autonomy rung; attached to the existing monthly GSC retainer ceremony (no new calendar slot) during bootstrap. Pipeline: kallregister rows due → change detection (machine-readable first: npm version diffs, RSS, errata-page diff; INTERNAL delegates to doctor) → delta extraction (changelog lines QUOTED verbatim, never paraphrased — AI-corruption mitigation) → materiality IGNORE|WATCH|REPRODUCE|EXPERIMENT|REQUIRED_UPDATE|KNOWLEDGE_CANDIDATE (tests in references/materialitet.md; REQUIRED_UPDATE must name the operative file:line it invalidates) → impact (grep claims for registry_ref + kallregister.styr paths; fail-soft: knowledge repo absent ⇒ impact ODÖMBAR, cycle still valid — prevents covert runtime dependency) → routing: REQUIRED_UPDATE→steward proposal (ONE intake surface) · KNOWLEDGE_CANDIDATE→claim at OBSERVED · EXPERIMENT→experiment proposal · WATCH→next cycle. Output: nortropic-knowledge/reports/radar/<YYYY-MM>.md, EXCEPTION-FIRST ("inget materiellt denna cykel" is a good result) + per-source consultation/outcome table (=the Source Alpha seam). Cadence within the monthly run: PLATFORM/PRACTICE monthly · NORMATIVE+CRAFT quarterly (CRAFT cycle = re-render premium-bevis exemplar URLs) · FRONTIER excluded (feeds own it). Owner budget ≤30 min; >5 REQUIRED_UPDATEs ⇒ rank + DEFER rest. Removal condition: 4 zero-yield cycles → quarterly; 4 more → retire to doctor #14 WARN only (REMOVED-ledger row). NORMATIVE promotion rule: proposals cite the source clause, never the claim file, as Belägg — the claim is a finding aid, never the authority chain.

 G. Garbage collector

 Extends the EXISTING Bibliotekarien retrostep with a knowledge sweep: claims stale past volatility · OBSERVED >6mo without second instance (archive) · kursplan modules uncited 2 projects · sources with 0 yield over 6 cycles · unfilled Erfarenhet headings · unconsumed metadata fields. Safe removals → nortropic-knowledge/operations/removed-ledger.md (the RFC's REMOVED ledger: what/why/restore/regression-watch). "Största hävstången" may name a REMOVAL as the biggest lever — deletion competes with addition. GC has explicit authority to propose removing pieces of THIS architecture.

 H. Curriculum compiler (CF-2 instantiation)

 v1 = formalize what exists: pack manifests carry moduler: (role → reference files); brief §7 selects packs; agents load conditionally (existing precedents). ONE hand-written page docs/kursplan.md (role × fixed skills × pack-conditional modules, "Senast verifierad" stamped, doctor #12 covers it). Audit trail = brief + kursplan + AGENT-LOG (reconstructable per project). kursplan-kostnadsvakten (new steward standing rule): module cited zero times in 2 consecutive projects' logs → propose demotion to on-demand (kanon-kostnadsvakten pattern; budgets from usage-log data only). FUTURE (build trigger: >2 packs AND an actual context-provenance dispute): the CF-2 deterministic resolver (task+role+SHA → hash-identified context artifact; naming per §10.1 stays an owner decision).

 I–K. Experience layer + generalization + failure-to-competency

 First-project capture: one mandatory section, not a telemetry system — ## Erfarenhet in the existing per-project STEWARD-REPORT (no new file; write-policy untouched): Kontext · Strategibeslut (3-7 rader from AGENT-LOG, not rewritten) · Större fynd · Kundkorrigeringar · Misslyckanden (incl. REVIEWER BLIND SPOT rows) · Post-launch-hypoteser (falsifiable, WITH kontrolldatum; outcome filled at next retro or left ODÖMBART — no analytics readback built) · Lärdomskandidater. Lesson-candidate format: one sentence + Giltighetsomfång (MANDATORY — "okänt" is valid) + belägg + ladder + rubrik-kriterium. Generalization = the EXISTING retro machinery unchanged: twice-across-projects → CORROBORATED → proposal carrying the scope line VERBATIM (widening must be argued explicitly) → owner applies → ADOPTED. Reviewer-blind-spot loop: production contradicts green review ⇒ BLIND SPOT row ⇒ mandatory candidate: new reviewer lens (§A3 HÖGRISK) | new eval sub-criterion (§A2 HÖGRISK MAJOR) | new frozen fixture (§A6 human-cut via --cut-baseline). Canonical record = customer repo; advisory narrative = learnings/<kund>-<datum>.md with repo@SHA pointer; NOTHING at build time reads learnings/.

 L–M. Competency registry + new-model-as-candidate

 docs/kompetensregister.md: competency = role-config × pack/capability, status vocabulary IDENTICAL to Part 1's capability statuses; evidence columns = EVAL-RESULT history, gate history, fixture/holdout coverage; at n≈1 most cells honestly read "1 datapunkt — ODÖMBART". Modellkandidatregeln (new standing rule, generalizing the Sonnet-trappan): ANY roster change, upgrade or downgrade, is a proposal showing (i) candidate vs frozen verify-suite fixtures, (ii) candidate vs held-out cases, (iii) rollback clause, (iv) usage-log cost delta; MODELLKONTRAKT changes one row at a time — never global hype-replacement. Held-out home (answers corpus open question): separate private repo nortropic-holdout — never cloned during builds, never in agent-readable paths, cloned transiently by the OWNER during explicit evaluation ceremonies (the human-deploy trust pattern); capability evals ≠ regression gates (gates keep depending only on nortropic-system fixtures). OWNER-DECISION.

 N–S. Golden corpus · R&D · experiments · health · provenance · conflicts

 - Golden corpus: premium-bevis mechanism VERBATIM per pack (packs/<n>/references/bevis.md, same laws), populated only on demonstrated dispute (its own precedent). Negative exemplars = ## Motbevis appendix in design-blocklist.md (one-home: blocklist owns the sins): cluster-id + live URL + dated render + one-line distillate; build on first builder/reviewer dispute; sourced from deceptive.design taxonomy + AI-slop taxonomies (T-3).
 - R&D lane + experiments: Observatory experiment discipline on TESTKLIENT fixtures only, never production customers; record in research/experiment-*.md (hypothesis, protocol, baseline/variant rubric scores, outcome REJECT|WATCH|REPRODUCE|ADOPT_SCOPED|PRACTICE_CANDIDATE; ADOPT_SCOPED carries Giltighetsomfång). Budget ≤1/month and only via radar/retro EXPERIMENT routing — no standing research program. Dormant by default.
 - Health: exception-first lines in STEWARD-REPORT (stale claims N · öppna konflikter N · radar-cykel försenad · holdout-täckning saknas för <packs>). NO composite score. Verkstadsgolvet surface FUTURE, read-only projection, not before the lines exist 3 retros.
 - Provenance: claims LINK (external_sources.locator + clause anchor); operative homes point per "peka, återge aldrig"; the chain CURRENT RULE→EVIDENCE→SOURCE→VERSION→SCOPE→DERIVED EVAL→EXPERIENCE is walkable via claim frontmatter + operative_home + registry_ref.
 - Conflicts (amendment #7 — QUESTION-DEPENDENT AUTHORITY, not a global total ordering): authority depends on the question being asked — NORMATIVE sources govern STANDARDS · PLATFORM/EMPIRICAL sources govern OBSERVED IMPLEMENTATION REALITY (a spec claim loses to measured browser behavior on "what actually happens") · LEGAL sources govern OBLIGATIONS · PROFESSIONAL RESEARCH governs PRACTICE EVIDENCE · scoped Nortropic production evidence may CHALLENGE practice/craft within its Giltighetsomfång, never normative/legal. No forced false total ordering — cross-class disagreements are PRESERVED as conflicts (slots under authority-order rule 4; brief §5/§7 still outranks for a given project). Unresolved → bok/konflikter/KONFLIKT-<id>.md, both positions verbatim-sourced, status OPEN|RESOLVED|WONT_RESOLVE (legitimate terminal — "we do not know yet"); a claim with an OPEN konflikt cannot reach ADOPTED. Seed with T-2's contested-practice register.

 U–V. Freshness cadence + first-project capture

dence per source class: from T-1's verified volatility table (PLATFORM/PRACTICE monthly-automated; NORMATIVE semi-annual/quarterly; CRAFT 2x/year deep + weekly minutes-scale gallery skim with gated intake; FRONTIER monthly scan, quarterly triage, never auto-adopt; legal event-driven + monthly IMY skim during enforcement wave). First-project capture = §Erfarenhet (I above) — active from the FIRST customer under the new system.

 W–Y. Knowledge red team / not-yet / smallest experiment

 Owner steady-state cost of the whole layer: ~30 min/month (radar) + ~20 min/retro — everything else dormant-by-default; GC explicitly authorized to shrink the architecture itself; every component carries a removal condition. NOT YET: Source Alpha computation · Context-Manifest resolver · People Radar · feed consolidation · analytics readback · verkstadsgolvet surface · negative exemplars before first dispute · per-pack bevis before disputes · new agents · dashboards/scores · holdout content beyond ~3 cases. Smallest E2E knowledge experiment (runnable, <1h owner time): radar leg — one /nortropic-radar cycle on PLATFORM+PRACTICE (diff web-features + BCD versions, read Chrome notes + Search Central since fixed date) → expect 0-2 non-IGNORE → one proposal → owner gates → one operative commit → claim OBSERVED→ADOPTED with operative_home set (note: T-1 already found a live candidate — the FAQPage deprecation vs our own FaqSchema/criterion-6 language). Experience leg — first real retro writes §Erfarenhet with ≥1 scoped lesson; its bransch-antislop finding flows through EXISTING N1 Zon-1 harvest → next project loads it automatically. Success = both legs produce one gated update each, consumed by a later build, zero new always-on context.

 T. Literature/industry research findings

 T-1. Normative & platform sources (verified live 2026-08-24)

 Headline: the "no daily web search" constraint holds — 11 of 13 domains are coverable by RSS/Atom/npm/GitHub feeds or calendar events; only IMY/EU-legal and ETSI need periodic human page-checks. Per-vendor browser watching is now infeasible (Chrome moves to 2-week cadence from v153 Sept 2026, Firefox biweekly from v155) — the radar must consume AGGREGATED signals.

 Top 5 feeds by signal-per-effort (ranked, verified):
 1. web-features npm package (~weekly releases; every feature carries Baseline status + dates; diffing two versions = exact machine-readable "newly Baseline" list). SUBSTITUTES for watching WHATWG+CSSWG+TC39+all three browser vendors individually. Governs what generated code may use.
 2. Google Search Central docs changelog RSS (developers.google.com/search/updates/search_docs_updates.rss) — weekly entries; structured-data deprecations land here first.
 3. Next.js releases.atom + GitHub Security Advisories — the only feed needing <48h reaction (cf. CVE-2025-29927 middleware bypass). Support policy verified: major = Active until next major, then ~1yr security-only; Next 15 EOL 2026-10-21 — the factory's pinned create-next-app@15 scaffold falls out of security support in under 2 months (IMMEDIATE operational finding).
 4. IMY news + Digital Omnibus tracking (monthly manual skim — no machine feed). ePrivacy withdrawn Feb 2025; Digital Omnibus (Nov 2025) rewriting cookie rules into GDPR Art 88a/b; IMY issuing 300k–12M SEK fines for deceptive cookie banners late 2025. Factory mitigation: genuinely cookie-free sites convert this HIGH-volatility legal domain to LOW (already house default).
 5. WebKit "Safari 26.x features" blog RSS — the compatibility floor, ~6-weekly, best-written vendor digest.

 Volatility classes (verified): LOW/annual = IETF, TC39 (June, 1 event/yr — ES2026: Temporal + using), OWASP (Top10 2025 final Jan 2026 — new: Software Supply Chain Failures, directly relevant to npm factory; quiet until ~2028), WHATWG-as-spec (Review Drafts Jan/Jul; substance arrives via Baseline), WCAG 2.2 text, WCAG 3.0 (new WD Mar 2026; CR ~Q4 2027 — ignorable for production until ≥2027), Bing/IndexNow (implement ping-on-deploy once; 22% of clicked search URLs originate from IndexNow, feeds ChatGPT/Copilot retrieval). MEDIUM/quarterly = CWV/CrUX (set unchanged: LCP 2.5s/INP 200ms/CLS 0.1; no 4th metric; soft-navigation CrUX reporting "to be determined" — and beware: SEO-blog claims of a "tightened INP methodology" could NOT be verified against Google primary sources, a live calibration example for per-source trust), Interop 2026 (20 focus areas incl. anchor positioning + view transitions on the 1-2yr runway), EN 301 549 v4.1.x aligning to WCAG 2.2 expected 2026 (THE scheduled a11y radar item for a Swedish factory; EAA enforceable since 2025-06-28, PTS actively surveilling), ARIA APG. HIGH/continuous-automated = Search Central, web-features, Next.js/Vercel, browser deprecations, Swedish/EU cookie law during the current enforcement wave.

 Concrete stale-knowledge failures (verified — what a 2024-frozen BoK would get wrong): FAQPage JSON-LD — Google stopped showing FAQ rich results entirely 2026-05-07, Search Console report + Rich Results Test removed June 2026 (NOTE: the factory's FaqSchema component and rubric criterion 6 FAQPage-filtering language predate this — an immediate REQUIRED_UPDATE radar finding against our own standard); seven structured-data types retired June 2025; Privacy Sandbox APIs shut down Oct 2025, removed by Chrome 150 July 2026, while third-party cookies SURVIVED — platform-empirical reversals arrive faster than any normative document changes, which is why tiers are only a cold-start prior. Newly-Baseline 2026 features that invalidate scaffold workarounds: @scope, container style queries, field-sizing (kills JS textarea autogrow), contrast-color(), Navigation API, :open.

 T-2. Professional practice + literature canon (verified live 2026-08)

 Evidence bases, ranked by usefulness to this factory:
 - GOV.UK Service Manual + Design System + content design — verified OGL v3.0 / MIT: the only rigorous corpus legally ingestible into the BoK verbatim-with-attribution (forms: one-thing-per-page, error messages, question protocol; content design; a11y; research methods). Highest license value, zero cost. USWDS = CC0 secondary.
 - Baymard — free plan (50 guidelines) since Feb 2025; premium covers B2B/SaaS/insurance/travel but all transactional-flow research; NO lead-gen/local-service coverage exists anywhere (verified by absence) — a genuine market gap that Nortropic's own consistently-instrumented experience layer is positioned to fill IF provenance discipline (claim→source→context→date→confidence) exists from day one. Baymard's institutional form (per-guideline test provenance, benchmark DB, continuous re-testing, explicit context boundaries) is the TEMPLATE for Nortropic's evidence layer.
 - NN/g — directional heuristics, small-n, never peer-reviewed; cite "NN/g" and "Nielsen solo post-2023" as SEPARATE authorities (Nielsen left 2023; his solo writing must not inherit NN/g authority). F-pattern contested (EyeQuant quantitative data finds no F-bias). Annual re-check of load-bearing citations older than ~5yr.
 - WebAIM Million 2026 (verified): 95.9% of top-1M home pages fail WCAG (FIRST REGRESSION in 6 years, attributed partly to AI-assisted coding); six error categories = 96% of all errors for 7 years running (low-contrast 83.9%, missing alt 53.1%, form labels 51%, empty links 46.3%, empty buttons 30.6%, missing lang 13.5%). A "zero WebAIM-six" standing gate is cheap and differentiating — an AI factory must prove it beats, not feeds, this trend.
 - Automated a11y coverage claim adjudicated: GDS 2017 = best tool ~40% of issue TYPES; Deque 2021 = 57% of issue VOLUME. BoK formulation: "roughly a third of types, half of volume; keyboard + screen-reader passes mandatory for the rest."
 - Swedish layer: klarspråk corpus (Språkrådet, Klarspråkstestet, Myndigheternas skrivregler) = first-class BoK source; LIX as smoke test only (Swedish compounding biases it — verified in Swedish NLP literature).
 - CRO-statistics ecosystem is actively contaminated (verified by direct encounter with fabricated content-farm statistics attributed to real orgs). BoK rule: no conversion statistic enters without primary-source URL + methodology note.

 Literature canon (~25 works, each with staleness audit — full list in research transcript): Norman DOET (timeless vocabulary) · Krug ×2 (testing philosophy timeless, screenshots stale) · Cooper About Face 4 (posture/excise timeless; persona methodology has weak outcome evidence — communication tool only) · Johnson Mind in Mind 3e (safest "why" layer) · Bringhurst (print-first, needs web translation) · Butterick Practical Typography (best web-native typography text; free online, not redistributable) · Müller-Brockmann (read for eye, not method) · Wathan/Schoger Refactoring UI (highest visual-competence leverage per page for a designer-less factory; directly encodable as agent rules) · Frost Atomic Design · polar bear IA 4e (extract labeling+navigation chapters; oversized for 5-30-page sites) · Covert (right-sized IA) · Redish · Winters/Edwards Content Design 2e (the process book for content-first sites) · McGovern Top Tasks (scale method down: owner interviews + call/email logs + search queries as task evidence) · forms stack = Wroblewski (framing timeless, pre-mobile specifics stale; 2009 inline-validation study overgeneralized — GOV.UK validates on submit) + Jarrett question protocol + Silver Form Design Patterns + GOV.UK living patterns · Hall Just Enough Research (the correct calibration for one operator) · Portigal · Kohavi (bought as IMMUNE SYSTEM: winner's curse, p-hacking — the authoritative reason NOT to A/B test at SMB traffic) · Kahneman (priming chapter effectively retracted — replication check required for any behavioral claim) · Stickdorn service design · Ousterhout (unusually apt: interface clarity is what keeps agents effective in an agent-maintained codebase) · Souders (HISTORY ONLY — specific rules now stale-to-harmful under HTTP/2-3; culture lesson survives) · Pickering Inclusive Components. Keep off the shelf/annotate: Weinschenk, GoodUI-as-guidelines, Cialdini magnitudes, $300M-button lore, Hick's/Miller's misapplied to menus.

 Shelf-life model (adopt as the BoK's knowledge-class taxonomy — the timeless-vs-current split operationalized): 1 human cognition (decades; replication check on admission) · 2 research methods (decades) · 3 design principles (decades, fashion-inflected; 3-5yr) · 4 interaction conventions (2-5yr; annual) · 5 population-tested guidelines (1-3yr; annual, anchored on Million report + Web Almanac releases) · 6 platform/implementation facts (months; quarterly; NEVER stored as static claims — stored as "fact as of DATE, source URL, re-verify at use") · 7 legal/regulatory (event-driven). Every claim carries its class tag.

 Five highest-value acquisitions: GOV.UK corpus (free, ingestible) · Refactoring UI · Content Design 2e + klarspråk (free) · Baymard free-now-premium-at-trigger · Kohavi. Free mentions: Practical Typography, Atomic Design, TetraLogical Foundations, axe-core + Deque University rules, Web Almanac, NN/g articles.

 Contested-practice register (encode as KONFLIKT records, not rules): inline-vs-submit validation (encode both positions) · above-the-fold and carousel doctrine (softer than claimed) · personas · behavioral-psych effect magnitudes · published A/B "wins" (hypothesis generators, never guidelines).

 T-3. Elite craft + frontier AI (verified live 2026-08)

 Studios: elite reputation and published method are almost uncorrelated. Only three publish auditable process: AREA 17 (guides.area17.com — full internal method library: design/interface-engineering/production as parallel craft tracks with shared QA vocabulary = a direct template for BoK structure; + Pentagram case study with real architecture reasoning) · ustwo (Pixel Perfect Precision Handbook — craft quality as enumerable checkable items → agent QA gates) · Clearleft (research→design traceability studies = our "WHY stored with the reference" problem, empirically). Work & Co (absorbed into Accenture Song; historical reference: prototype-as-unit-of-discovery) · AKQA (only the MoJ/Optimal Workshop case has data: tree-testing 56%→72% task success) · Instrument (deliverable = system + guidebook) · Huge/Metalab/Fantasy publish nothing auditable. Locomotive = a documented homogenization vector (their scroll library propagated one aesthetic across the award ecosystem).

 Design signal sources: SiteInspire (single-curator editorial floor, no sponsored entries — but one person's taste = one homogenization axis) · Awwwards/CSSDA (ceiling calibration ONLY; verified: Design 40%/Content 10% weighting, winners "rarely navigable with keyboard", no a11y category, judged on high-end hardware — the factory's "prize rosette" stance is empirically supported) · Mobbin (shipped-product flows = UX research, not inspiration) · Land-book/Httpster (breadth). Corpus rules confirmed by the homogenization literature (meta-analysis 19 studies/61 effect sizes: small significant homogenization effect; Doshi & Hauser 2024: individual creativity up, collective diversity down; structured diversity prompting measurably restores variance): galleries are discovery feeds never intake; nothing enters without written WHY; tag provenance per entry — >N patterns from one provenance cluster = homogenization alarm; add tracking of INTER-SITE similarity across the factory's own portfolio (can homogenize against itself while avoiding the global cluster — step-9 lens already does this at n=2, keep at volume). Negative exemplars verified live: deceptive.design Hall of Shame (typed taxonomy, updated 2025) · hallofshame.design · User Inyerface · Web Design Museum/Version Museum (why patterns died).

 AI-slop convergence (directly validates the antislop stack): the cliché taxonomy is convergent across sources (Inter/Space Grotesk, indigo-purple gradients, glow borders, card grids, orbs, badge-pill+two-button hero); root cause traced — LLMs emit the training-data median = Tailwind defaults + shadcn conventions (v0 explicitly trained on shadcn); the feedback loop is now in print ("a pattern that looked original in early 2025 was a cliché by mid-2026"); detection tooling exists (Hallmark linter, 65 slop-gates — evaluate or replicate as an in-house mechanical gate feeding the design-blocklist).

 Frontier maturity assessment: ADOPT (scoped) — agent-driven browser testing via Playwright MCP/accessibility-tree snapshots (production-adopted industry-wide; the highest-confidence frontier adoption: agent smoke/flow tests per client site pre-ship). ADOPT-with-mitigations — LLM-as-judge (self-preference bias real and correlates with self-recognition [NeurIPS 2024]; position bias flips verdicts; mitigations: different family than generator, order randomization + agreement, rubric-anchored not open preference, periodic human calibration; ~80% human-agreement achievable = good enough for gating, NOT for taste). EARLY — screenshot-diff + MLLM commentary as regression/sanity gate ("did the hero break at 375px"); Design2Code: models "mostly lag in recalling visual elements and generating correct layout". NEVER — MLLM aesthetic scoring as taste arbiter: the judge's taste IS the homogenized median we fight. Hybrid-only — a11y automation (research consensus: undercounts; keyboard/screen-reader passes stay). WATCH — generative/adaptive runtime UI (fragility + a11y risk, near-zero SMB value). Anthropic engineering canon current set verified (Building Effective Agents, Context Engineering, Writing Tools, Demystifying Evals Jan 2026, AI-resistant evaluations Jan 2026); OpenAI cookbook equivalents; OpenAI hosted Evals platform shuts down Nov 2026 — never build on it (corroborates DO-NOT-BUILD #20).

 AI-mediated discovery (measured, for client conversations): AI Overviews suppress clicks 35-58% on affected queries (Pew: 8% vs 15% CTR; ~48% of tracked queries carry AIOs); AI referrals real but tiny (0.3-0.5% of traffic, ChatGPT 75-92% of it); crawl-to-refer ratios brutal (Anthropic ~1900-4600:1, OpenAI ~850:1) — allowing AI crawlers is a defensible BET on assistant-mediated recommendation for "bästa rörmokaren i Västerås"-class queries, state it as a bet. llms.txt effectively dead (Google won't support; 97% of files get zero bot traffic) — never a client deliverable. schema.org: keep for classic reasons + assistant grounding; "not required for AI search" per Google. NLWeb: WATCH, 2-3yr horizon. Cargo-cult register (do not adopt): llms.txt deliverables · GEO/AEO vendor platforms & AI-visibility scores · runtime generative UI on client sites · multi-agent-by-default (both labs say start simple) · MLLM taste arbiter · "AI WCAG certification" · NLWeb for SMBs · award-chasing · auto-retraining self-evolving loops without human diff review.

 Craft/frontier cadence: studios 2x/year deep sweep + event-driven acquisition news; frontier engineering monthly scan/quarterly triage into ADOPT-scoped vs WATCH; galleries weekly minutes-scale skim with gated intake + quarterly provenance-cluster review; AI-discovery evidence quarterly (only measured-behavior sources reset policy). Everything here generates HYPOTHESES for the R&D lane — nothing bypasses it.

 Part 2b — Cross-model workforce evolution (directive III, 2026-08-24)

 Scope

 Provider-neutral workforce: MODEL ≠ ROLE ≠ WORKER CONFIGURATION ≠ COMPETENCY ≠ EXPERIENCE. New model = candidate employee, role-specific promotion, never global upgrade. Cross-model review, blind + reciprocal judging, judge competency + calibration, disagreement-as-evidence, grader evidence hierarchy, practice/benchmark/hidden-holdout separation, synthetic company sparring, failure-driven training, production-failure→sanitized-gym-case, promotion policy, cost/latency/quality routing, GYM_AUTONOMY (bounded owner command → autonomous improvement session → management report). Student can NEVER move goalposts. Terminology note: "Web Gym"/"Practice Bank"/"Benchmark Bank"/"Synthetic Company Generator" are directive-III names for lanes the corpus RFC calls Gauntlet (CF-3, quality loop inside the trust loop) and Experiment Engine (CF-4); the plan harmonizes both vocabularies — OWNER-DECISION on canonical naming.

 Repo evidence: the pattern already exists in embryo (VERIFIED earlier this session)

 - Cross-model workforce IS the current control plane: Claude builds product slices; Codex (OpenAI gpt-5.6-sol) runs architect/test-author/gate-reviewer/builder/reviewer/empirical-runner roles (.agents/skills/*, domain-free, provider-neutral role contracts); owner rule "Codex granskar varje PR som rör §A-ytan" = cross-family review of the highest-risk surface is already house practice. harness-substitution-contract-v1.md = the provider-neutral contract (substitution test, 11 never-delegated trust responsibilities); provider identity SHA-pinned (codex-provider-identity.json). Corpus contradiction #3 resolution shape, owner-endorsed: neutral interface + provider adapters.
 - Worker-configuration registry embryo: agents' frontmatter model·effort + MODELLKONTRAKT in steward SYSTEM MAP, doctor #8 fells silent deviations = the incumbent registry. Sonnet-trappan (model change requires ≥2 consecutive clients eval≥90 + zero gate misses + rollback clause) + standing rule "Fable-lyft för copy" = evidence-gated promotion/demotion ALREADY law. "New model ≠ automatically senior" is existing house doctrine, needs extension to upgrades not just downgrades.
 - Student-cannot-move-goalposts is already implemented in the control plane: task envelope deliberately omits exit_test so the worker cannot tune against its own gate (h-008); gate-reviewer role hunts vacuous gates; §A protects rubric/gate severities; tests/fixtures human-cut. The gym inherits this pattern, not a new invention.
 - Judge-calibration embryo: verify-kalibrering.md (mechanical decision rules for skeptic count/scope), REVIEW-REPORT-CALIBRATION.md (--no-verify calibration runs), §B8 Goodhart meta-review, eval-baseline as frozen judge-anchoring fixture.
 - Synthetic company embryo: TESTKLIENT machinery (fabricated-but-labeled fixtures, noindex enforced, never-claimable) + Case B paper-pipeline fixtures = the Synthetic Company Generator's substrate.
 - Binding corpus constraints: DO-NOT-BUILD #6 (no symmetric N-round debate; asymmetric challenger favored per measured evidence) · #7 (critic fan-out pays off at the capability edge only) · #27 (budget exhaustion ≠ PASS) · CF-3 (critics misdiagnose root cause — Shumer evidence; self-preference/position bias "measured phenomena"; evaluator contracts WITHOUT verdicts; "Nortropic does not become a Gauntlet loop — it contains one"; self-improvement = self-PROPOSAL of stronger measurements, authority freezes them) · frontier-delta (multi-run/shuffled protocol, optimizer-invisible held-outs).
 - GYM_AUTONOMY governance precedent: the build-autopilot's owner-bounded delegation (remaining-bootstrap-delegation-v1.md: OWNER_*_APPROVAL_REQUIRED=NO for a bounded chain with every mechanical precondition kept; HUMAN_AUTHORITY_HARD_STOP reserved) — the gym session is the same shape: bounded owner command, mechanical preconditions, promotion through normal trust path, never touching §A.

 Governing decisions (the gym spine)

 - GD1 No parallel trust machinery — every promotion is an ordinary steward proposal / owner HÖGRISK editing the EXISTING registries; the gym produces evidence files, never verdicts with authority (MODEL_OUTPUT_IS_TRUST_AUTHORITY=NO, judge output included).
 - GD2 The goalposts are §A; the gym adds exactly ONE new §A entry (§A9 below). Mutable: worker prompts, kursplan selection, examples, model choice, routing rows, challenger configs, practice bank. Frozen: rubric (§A2), gate semantics (§A3), suite+baselines+gym benchmark+anchors (§A6 by placement), holdout definitions/answers (repo-level envelope), grader authority/judge rules/blind protocol/promotion policy (§A9), security/a11y kravnivåer.
 - GD3 Placement inherits protection — benchmark bank + calibration anchors live under tests/fixtures/gym/ (already §A6, human-cut via --cut-baseline).
 - GD4 Synthetic ceiling law — synthetic evidence carries a competency/worker-config to at most VALIDATING; PROVEN always requires real customers at the Sonnet-trappan threshold. Kills benchmark-Goodhart and synthetic-unrealism in one sentence.
 - GD5 Deterministic first, judge last — a krav graded at level N may never be re-graded lower to turn red green.
 - GD6 OpenAI access = the pinned Codex toolchain (SHA-pinned identity, structured output, evidence contract) — no hypothetical APIs, no provider-hosted eval platforms.

 A–C. Worker configuration model — extend, no new registry

 Worker config = tuple (role contract file @ commit · MODELLKONTRAKT row · kursplan row · pack modules per brief); config-ID = <roll>@<leverantör>/<modell>. MODELLKONTRAKT gains two columns: leverantör (default anthropic) + identitet (pointer to config/<provider>-provider-identity.json); doctor #8 extended: non-Anthropic row without SHA-pinned identity = FAIL. Challenger configs (gym-only, never production): gym/utmanare/<id>.md — agents-frontmatter schema + leverantör, variant_av, varierad_variabel (modell|rollprompt|kursplan|retrieval|exempel|arbetsflode|komposition — ONE variable per challenger; komposition needs explicit owner flag). Provider adapter, factory side (substitution test answered): an OpenAI strategist/reviewer needs only (1) the existing provider-free role prose, (2) the pinned identity file, (3) an output schema — FINDINGS schema, §7-brief structure, EVAL-RESULT format all exist; the gym adds one: the judge schema. Competency link: docs/kompetensregister.md admits worker-config × capability and judge-config × dimension rows — same status vocabulary, same file.

 D. Candidate employee onboarding (kandidat-ceremoni)

 New model = one-variable challenger. Ceremony: (1) challenger file → (2) frozen-suite leg (BLOCKED-ODÖMBAR until S0, honestly recorded) → (3) benchmark leg (tests/fixtures/gym/, deterministic graders first) → (4) holdout leg (owner ceremony, candidate never sees answers) → (5) evidence table + proposal: ONE MODELLKONTRAKT row, Sonnet-trappan-verbatim rollback clause, usage-log cost delta. Role-specific evidence tables defined (planner: kontrollrad compliance + §7 completeness + facit capability-mapping + marker greps + strategy dims judged; content-designer: antislop greps + faktatrohet vs facit + copy dims judged — the highest-value cross-family surface; seo/qa: deterministic suffices, no judging).

 E–F. Cross-model review + blind/reciprocal judging

 Cross-family pays ONLY on: subjective/stylistic dims (copy, voice, visual-distinction description), strategy quality, §A-class surfaces (already house law — Codex reviews §A PRs). Deterministic dims never get a second family. Blind protocol (blind-protokoll.md + gym/verktyg/anonymisera.js): strip frontmatter/metadata, normalize formatting, grep-and-ABORT on provider-identifying strings (fail-closed, never silent redaction), relabel KANDIDAT-A/B by recorded coin flip; mapping.json envelope-withheld from judges (h-008 pattern), read only at aggregation. Honesty clause: style fingerprints make blinding imperfect — so it is PAIRED with decomposed rubric anchoring + the measured family-preference statistic (we measure the leak, not pretend it's sealed). Reciprocal matrix, DO-NOT-BUILD-#6-compliant: both families produce on the same fixture, both judge both artifacts (INCLUDING own family — deliberately, to measure self-preference), SINGLE round, order-randomized, rubric-anchored, decomposed per dimension — reciprocal judging of artifacts is not debate. Judge output schema (CF-3 "evaluator contracts without verdicts"): per-dimension DOMSLUT rows (dimension-id · 0–4 vs anchor · verbatim evidence quote · osäkerhet); FORBIDDEN: total scores, winners, recommendations, provider guesses. Aggregation is mechanical/owner-side. Disagreement: JUDGE_DISAGREEMENT files (KONFLIKT shape), NEVER averaged; escalation ladder: rubric inspection → third judge (that dimension only) → deterministic evidence → synthetic-user eval → human calibration sample → "inherently subjective" = WONT_RESOLVE (dimension excluded from promotion math, logged as rubric-quality finding).

 G–J. Judge competency + grader hierarchy

 Judge role contract .agents/skills/nortropic-domare/SKILL.md (provider-neutral, read-only, gate-reviewer-patterned: lock artifact identity, per-dimension findings with evidence, never verdict, never repair). Calibration (domar-kalibrering.md, extends verify-kalibrering mechanics): anchors in tests/fixtures/gym/ankare/ — known-good (frozen rorjour material, Case A brief) + known-bad (one strawman per subjective dimension, violating exactly that dimension); mechanical pass rules: 0 good/bad inversions · 3-run shuffled stability (max 1-point drift) · human-sample agreement ±1 step · family preference reported. Judge status per dimension: BUILT→VALIDATING (passed calibration)→PROVEN (≥2 sessions unoverturned); demotion on calibration fail or overturned-by-deterministic-evidence. Evidence hierarchy unified with Part 1's execution classes (one table): DETERMINISTIC=MECHANICAL > BROWSER-OUTCOME=BROWSER-SYNTHETIC > PROTECTED-SYNTHETIC-USER (facit withheld) > CALIBRATED-JUDGE=INDEPENDENT-AI with register status ≥VALIDATING for that dimension > UNCALIBRATED-OPINION (admissible only as hypothesis) — human above all for promotions. Self-preference mitigation: different-family judge REQUIRED only for subjective AND promotion-relevant dims; breadth decided by measured preference, not doctrine.

 K–L. Bank isolation + synthetic sparring

 Three banks: practice gym/bank/ovning/ (fully optimizer-visible, mutable) · benchmark tests/fixtures/gym/ (§A6 by placement; per-answer error analysis in worker-facing artifacts FORBIDDEN — aggregate deltas only) · hidden holdout nortropic-holdout repo (never in agent-readable paths; owner clones transiently at ceremonies). Synthetic Company Generator = a generator RECIPE (not an agent): output contract = research contract v3 EXACTLY (fixtures pipeline-native, INPUT-GATE-consumable) + TESTKLIENT header block + facit in a separate envelope-withheld file (planted signals, expected primärutfall, planted contradictions — the deterministic graders' ground truth). Contamination defenses: generated+mutated cases, never real orgs, never public benchmarks; holdout grading details live only in the holdout repo; leak ⇒ regenerate, never reuse. Sparring: two producers max per fixture (no swarms) → deterministic graders vs facit → blind reciprocal judging on subjective dims only.

 M–N. Failure-driven training + sanitization

 Session reports cluster failures by underlying concept, never by count (manual in v1; clustering code NOT-YET). Sanitization pipeline: STEWARD-REPORT Misslyckanden/BLIND-SPOT rows → identity-stripped general pattern gym/bank/monster/M-<id>.md → mechanical privacy grep before commit (kund-slug, orgnr, phone patterns, domain — hit = FAIL) → generator produces N fictional variants in DIFFERENT verticals (tests the concept, not the memory) → practice bank. Law: customer repos never become fixtures; patterns do.

 O–P. Promotion policy + the goalpost law

 Promotion to production requires (role-weighted): benchmark improvement (aggregate) + holdout generalization (benchmark-up + holdout-flat = overfit verdict, NOT promotion) + per-dimension no-regression floor + independent cross-model review of the comparison evidence + mechanical compliance (doctor green, suite non-degrading) + ≥3 shuffled stable trials + acceptable cost/latency (usage-log rows) — through the NORMAL path. Demotion mirror = Sonnet-trappan rollback verbatim. NEW §A9 (one row, HÖGRISK, human): "Gymmets måttstockar" — docs/gym/gym-kontrakt.md (promotion policy, evidence hierarchy, judge rules, blind protocol, bank-isolation + privacy laws) + the holdout-access rule; doctor #5 grep extended to the path.

 Q–R. Routing + adapters

 Routing STATIC in v1 — the MODELLKONTRAKT matrix IS the routing table; rows change only by ceremony-backed proposal. Dynamic routing = FUTURE, trigger: ≥2 roles × ≥3 sessions of stable per-provider evidence AND a mechanical consumer. Adapters: neutral role contracts (doctor-greppable rule: no provider names in role-contract prose) + per-provider SHA-pinned identity files; provider #3 = one identity file + one ceremony, zero role-contract changes.

 S–U. GYM_AUTONOMY (staged) + management report

 Session command owner-bounded: fokus · case-tak (≤10 v1) · token-tak · utmanare. Session allowed_write = gym/** + steward-proposals ONLY — mechanically unable to touch §A, graders, or production agents. Stage 1 (v1): owner-triggered, every promotion human-gated. Stage 2 (v2, own delegation doc copying remaining-bootstrap-delegation anatomy; trigger ≥3 clean stage-1 sessions): auto-promotion of LOW-RISK surfaces only (role-prose wording, kursplan selection — never model changes, never judge/reviewer configs), governed by §B verbatim (digest rows, checkpoint cap 3, regression law with auto-revert + AUTO-INCIDENT, §B8 Goodhart question: "did gym numbers improve while sites didn't?"). Flags: GYM_TOUCHES_SECTION_A=NEVER · GYM_SEES_HOLDOUT_ANSWERS=NEVER · GYM_MODIFIES_GRADERS=NEVER · SELF_CERTIFICATION_AS_PROOF=NO. Stage 3 (FUTURE): trigger ≥6 clean stage-2 sessions + owner decision. GYM-RAPPORT (exception-first): Fokus · Fall körda · Utmanare · Utfall · Holdout-delta · Regressioner · Kostnad · Svaghetskluster · Nästa fokus · "Inget kräver ägarens uppmärksamhet" when true. Owner read ≤10 min.

 X. Smallest cross-model experiment — GYM-EXP-1 (the v1 payload)

 8 fictional companies (≥4 org-type axes, facit each; 6 practice-visible, 2 holdout — holdout repo created in this ceremony). Incumbent project-planner@anthropic/fable vs challenger planner@openai-codex (varierad_variabel: modell, identical role prose). Deterministic graders on all 16 artifacts (kontrollrad, §7 completeness, facit capability-mapping, primärutfall, marker greps) → blind reciprocal judging on exactly 4 subjective dims (strategy coherence, top-task user-fit, röstregister quality, differentiation vs planted competitors) → owner blind-scores 2 pairs (calibration) → mechanical aggregation, disagreement records, family-preference stats, GYM-RAPPORT + cost rows. Pre-registered pass/fail (the student can't move these either): SIGNAL = challenger better on ≥1 deterministic aggregate OR ≥2 judged dims (both-family or human-confirmed) AND direction holds on both holdouts AND cost acceptable → full candidate ceremony for strategist role only. HONEST NULL (first-class success) = no material holdout-generalizing difference → "second family adds cost but no production signal; reserve cross-model for §A review as today"; gym contracts stay (dormant costs nothing); re-test trigger = next major model generation. JUDGE-FAIL = anchor calibration fails or family preference exceeds pre-registered bound → NO workforce conclusions; judging becomes the next fokus. Budget: owner ≤4h, token cap in command; exceeded = ABORTERAD, never PASS.

 Gym v1 payload (one owner batch + one ceremony + one experiment)

 .gitignore rows (!/gym/, !/skills/nortropic-gym/, !/docs/gym/) · docs/gym/gym-kontrakt.md + §A9 row (HÖGRISK) + doctor #5 extension · MODELLKONTRAKT columns + doctor #8 extension · kompetensregister row types · skills/nortropic-gym/ (SKILL + 5 references + anonymisera.js) · .agents/skills/nortropic-domare/SKILL.md · tests/fixtures/gym/ankare/ (2 known-good + 4 strawmen, human-cut) · GYM-EXP-1. Dependencies: only the ceremony's frozen-suite leg waits on S0; GYM-EXP-1 needs only the generator + research-contract v3 draft (paper-compatible even pre-S1). v2 triggers named: benchmark growth (2 sessions), sanitization pipeline (first real production failure), content-designer ceremony (SIGNAL on copy dims or new model), stage 2 (≥3 clean sessions), verkstadsgolvet projection (3 reports). Gym NOT-YET: dynamic routing · provider #3 · judge ensembles >1/family/dim · clustering code · Elo/leaderboards (comparability theater at this n) · cross-provider production BUILDER (crosses into §A3-graded territory — own ceremony later) · auto-generated rubrics · symmetric debate · any gym scheduler.

 Evidence annex (§29 research, verified against primary sources 2026-08; ESTABLISHED/FRONTIER/HYPOTHESIS-labeled in the research transcript)

 Findings that CHANGED the design above:
 1. The resample control (ESTABLISHED direction): Self-MoA beat mixed-family MoA; 9 frontier judges ≈ 2 effective votes (75% of nominal independence lost to correlated errors); best single judge matched the panel. ⇒ apparent cross-model benefit must be measured AGAINST same-family resampling, not against nothing. GYM-EXP-1 gains a third arm (below).
 2. Reciprocity is asymmetric (FRONTIER, the one controlled cross-model review study, n=116): Claude reviewing Codex raised pass rate 71.6→89.7%; Codex reviewing Claude LOWERED it 91.4→82.8%. ⇒ judge competency is tracked PER-DIRECTION; one-directional review is a legitimate outcome.
 3. Pairwise vs pointwise flipped (FRONTIER 2025-26): pairwise flips ~35% under distractor manipulation and is more generator-gameable; use pointwise rubric-anchored scores for gates (the judge schema already is), pairwise+swap only for rankings; format disagreement = red flag.
 4. Consistency ≠ validity (ESTABLISHED): judges with >0.95 test-retest showed severe position bias; raw agreement without chance correction overstates judges by 33-41pp — measure both, use kappa/CAPA.
 5. Human calibration floor (ESTABLISHED): ≥30 adjudicated items; solo-operator mitigation = seeded objective defects carry the ground-truth load + owner re-adjudicates 20 items after a week to estimate own consistency + plug-in sensitivity/specificity correction.
 6. Grounded synthesis only (ESTABLISHED for code, HYPOTHESIS for web): synthetic tasks generalize when they are MUTATIONS OF REAL ARTIFACTS with verifiable ground truth (SWE-Gym/SWE-smith: +14-25pp with transfer); free-floating LLM-invented cases risk narrow-skill illusions (SWE-bench Illusion: 76% path-guessing on contaminated repos vs 53% off-benchmark). ⇒ generator cases = mutations of real research/site artifacts with SEEDED VERIFIABLE DEFECTS, not free inventions.
 7. Noise floor (ESTABLISHED, Anthropic primary): 6pp swing from infrastructure alone; "differences below 3pp deserve skepticism"; 3-5 trials/case/arm + paired analysis + sequential looks (O'Brien-Fleming at 20/35 cases).
 8. Reward-hacking mandate (ESTABLISHED, Anthropic/METR/OpenAI primaries): gaming 43× more common when the scoring function is visible; reward-hacking generalizes to sabotage; penalizing detected gaming teaches concealment. ⇒ optimizer-invisible graders/holdouts are empirically mandated, and the gym must NEVER apply optimization pressure against judge rationales (monitor-but-don't-punish).
 9. Routing reality (ESTABLISHED critique): fair unified evals collapse most routers toward simple baselines; OpenAI's own production router failed at GPT-5 launch. ⇒ static tiering correct at this scale; routing decisions need their own evals — reaffirms Q.
 Do-not-build-on register (claims circulating 2026 with no support): "more judges = more reliable" · "cross-vendor review boosts detection 40-60%" (SEO-farm figure, no peer-reviewed basis) · "mixing families always helps" · "diversity is a durable moat" (frontier errors growing MORE correlated per generation — re-measure per model generation) · "temp-0 judges are deterministic" (production serving not bit-reproducible) · "arena Elo is ground truth" (Leaderboard Illusion) · "metrics-up = safe improvement loop" (compatible with evaluator-gaming; only hidden rotated holdouts + human promotion gates distinguish).

 GYM-EXP-1 (REVISED per evidence annex — supersedes the two-arm sketch above)

 Three arms, paired, within-case: A = incumbent + same-family reviewer (self-review baseline) · B = incumbent + SECOND INDEPENDENT same-family reviewer (resample control — the arm that kills or saves cross-model) · C = incumbent + cross-family reviewer/falsifier (reciprocal direction logged separately). Cases: generator fixtures built as MUTATIONS of real artifacts with 3-5 seeded verifiable defects each (facit); scale per budget — start at 8-12 cases × 3 trials/arm with sequential looks, extensible toward the statistically comfortable 50/200-defect design if early signal is ambiguous. Metrics: per-reviewer true-defect recall + false-alarm rate vs seeded ground truth · unique-true-defect yield of C over B (headline) · Claude–GPT error correlation vs Claude–Claude resample correlation (CAPA) · swap-consistency + test-retest per judge · holdout end-quality · cost per unique defect. Human calibration: ≥30 adjudications of disputed/unseeded findings (seeded defects carry objective load; owner re-adjudicates 20 after a week). Pre-registered null (DON'T build cross-model broadly): C's unique-yield over B <10% relative AND holdout lift <3pp AND cross-family correlation not meaningfully lower than resample correlation. Kill signal: reciprocal direction lowers pass rate ⇒ drop reciprocity, keep one-directional. JUDGE-FAIL branch unchanged. Budget/abort rules unchanged.

 Part 2c — Self-maintaining professional capability system (directive IV, 2026-08-24)

 Scope

 Consolidates directives II+III into a broader system removing the owner's need for continuous manual ecosystem scouting. Two new first-class concepts: SPECIALKOMPETENS (professional know-how: skills/rule-packs/detectors/procedures/frameworks/corpora — NOT equated with GitHub repos; fragments evaluable independently where licensing permits) and INSTRUMENTARIUM (professional instruments: what a worker can observe/measure/verify/act through — MCP is ONE INTERFACE to an instrument, never the abstraction; tool and interface are separate concerns). Professional capability = model + role + theory/standards + specialkompetens + instrumentarium + curriculum + workflow + experience + evaluation + authority boundary. Optimization target: BEST VERIFIED PROFESSIONAL WORK CONFIGURATION (never most skills/MCPs). Required: provenance audit of current vendored skills (known concern: some upstreams unknown), Emil Kowalski gap audit as first lifecycle exemplar, capability radar extended to 5 lanes (knowledge/specialkompetens/instrument/model/workflow), MCP-as-trust-boundary authority classification, tool-interface competition (Playwright MCP vs CLI vs Test), marginal-contribution testing, ablation ("minimum sufficient high-performance configuration" — cleaner as well as smarter), pinned upstream-update pipeline (LATEST=CANDIDATE, never authority), four autonomous loops (learn-from-world / learn-from-work / train / forget), expanded GYM_AUTONOMY, owner experience = "Gym." Boundary: NOT a marketplace/plugin-manager/trends-dashboard; every component answers the five questions (failure prevented / consumer / evidence / cost / removal). SELF-IMPROVING ≠ SELF-AUTHORIZING — existing trust architecture governs all promotion.

 A. Professional capability model (extends Part 2b's worker configuration)

 Worker configuration tuple extends to: (role contract @ commit · MODELLKONTRAKT row · kursplan row · pack modules · specialkompetens set · instrumentarium set with interface choice per instrument · authority boundary). Two new registries, both extending EXISTING files (no new registry system):
 - Specialkompetens = what vendored-skills/ + skills/ already are. The lifecycle upgrade: every entry gets a kallregister row (klass INTERNAL/CRAFT/PRACTICE, andringsdetektion) + a provenance classification (VERIFIED_UPSTREAM / PARTIAL_PROVENANCE / UNKNOWN_UPSTREAM / LOCAL_NORTROPIC / SUPERSEDED / STALE) recorded in its VENDORED.md. Law: production-load-bearing specialkompetens must not remain UNKNOWN_UPSTREAM — resolve or formally adopt as LOCAL_NORTROPIC fork (owner decision per skill, recorded).
 - Instrumentarium = docs/instrumentarium.md (one table, doctor #12-stamped): instrument · interface(s) (MCP/CLI/library/API) · authority class · consumers · status (same vocabulary as capabilities). Instruments are NOT capabilities — a tool row without a consuming role + verifiable value is DEFER/REJECT (anti-marketplace law).

 B–D. Inventories + provenance debt (AUDITED 2026-08-24 — five structural findings)

 D0 — The install root moved and nothing followed. The SYSTEM MAP declares ~/.claude as install root; on this machine that is false (no agents/, skills/, vendored-skills/ there). Doctor #3 would FAIL ×9, doctor #9 has neither operand; every ~/.claude-anchored doctor command (#3/#5/#6/#9/#12) is dead. The repo's vendored-skills/ copies are now the ONLY copies — the facit has become the artifact, unguarded by the diff designed to protect it. (Same root cause as S0 — the factory ran on another machine.)

 D1 — Provenance is zero-depth: 8× UNKNOWN_UPSTREAM, 1× PARTIAL (frontend-design: "Anthropic plugin, pinnbar" but not pinned; LICENSE.txt referenced but never vendored), 0× VERIFIED. Eight VENDORED.md files carry the identical template sentence "exakt uppström okänd". Recoverable-but-unrecorded payload metadata exists: impeccable = Apache 2.0, v3.9.1, updates from impeccable.style; content-humanizer = MIT, v1.0.0, author Alireza Rezvani; web-design-guidelines = vercel, v1.0.0, names vercel-labs/web-interface-guidelines. Name mismatches: taste declares name: design-taste-frontend, soft-skill declares name: high-end-visual-design — invoked by directory name everywhere ⇒ fragile/non-resolving.

 D2 — Mutable-remote violations (the audit's CRITICAL rows):
 - web-design-guidelines is a 39-line WebFetch SHIM fetching its entire ruleset from vercel-labs/web-interface-guidelines@main at review time — AND its two consuming agents (design-reviewer, stack-builder) have no WebFetch tool ⇒ simultaneously the biggest freshness hole and a silent no-op in the canon.
 - impeccable ships a live self-updater: polls impeccable.style/api/version, instructs npx impeccable update (unpinned), PRE-AUTHORIZED via allowed-tools: Bash(npx impeccable *); also npm install puppeteer on demand; its live-server injects script tags and rewrites CSP.
 - Unpinned npx: js-yaml (doctor #1), lighthouse (gates — scoring-weight changes between majors silently shift the ≥90/≥95 thresholds), prettier (every write). pnpm add gsap @gsap/react floating. create-next-app@15 = deliberate major-only pin (documented, minor/patch floats).
 - fal.run image generation SPENDS MONEY (model IDs pinned by name; vendor-mutable models; doctor #13's 90-day self-report is the only clock — and imagen-4-ultra's passed stanger date stays silent because the FAIL condition doesn't match).

 D3 — The vendoring promise is unenforceable: the Innehållshash in every VENDORED.md has NO verifier anywhere in the repo (grep verified); the only mechanical byte-integrity net is docs/100-dagar/baseline-manifest-20260730.sha256 (all 171 vendored files verify clean today) — and no doctor references it. Doctor #9 is a tamper detector (local↔local), NOT a freshness monitor; zero upstream-newness detection exists anywhere — the only upstream-aware code in the tree is impeccable's own self-updater.

 D4 — Instrumentarium reality vs declaration: 9 MCPs declared in agent frontmatter; exactly 1 (Trybloom) present in this session — doctor #4 degrades to KUNDE-EJ-KÖRAS. Authority map (verified): deploy+spend = Vercel CLI (launch fix-loop + autobygg); spend = fal.run + Trybloom (bemannat-only, correctly); send-real-email = Resend via lead.ts (recipient env-hardcoded, regel 10); irreversible external write = GSC script (human-only); everything else observes. Two gates rest on unbacked/duplicated tooling: axe-core is REFERENCED but never invoked by anything (Gate 4 = an unbacked assertion resting on the agent improvising an injection path); Lighthouse has TWO invocation paths (chrome-devtools MCP lighthouse_audit vs npx lighthouse) with different failure modes and no canonical choice. gh CLI: live contradiction — nortropic-stack SKILL still documents gh repo create while Repo-GRIND forbids it.

 D5 — Emil local content (for the gap audit): emil-design-eng = 679-line design-engineering/motion textbook — deep on construction (4-question decision framework, springs, transforms, clip-path, gestures, performance, a11y), review (mandatory format + 11-row checklist), motion vocabulary (easing/duration/stagger laws: never ease-in, <300ms, never scale(0), never animate keyboard actions); NOT covered: UI library selection. Instructed to emit an animations.dev course-marketing line in first responses (client-facing-surface concern). find-animation-opportunities = 132-line restraint-first triage (the Gate: frequency/purpose/speed/function; mandatory rejected-candidates section) citing Emil's "You Don't Need Animations". Four dangling skill references: both point at improve-animations/review-animations, gsap-build points at improve-animations/apple-design — none exist locally. Motion stack is cleanly stratified (regel 8 = whether/which · find-anim = where · emil = how it feels · gsap-build = how to type it) with only intended duplication. Hard coupling flag: premium-bevis PK-6 cites emil-design-eng:72–79 and :108 BY LINE NUMBER — any re-vendor silently breaks the citation.
───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 Claude has written up a plan and is ready to execute. Would you like to proceed?

 ❯ 1. Yes, and use auto mode
   2. Yes, manually approve edits
   3. Tell Claude what to change
      shift+tab to approve with this feedback